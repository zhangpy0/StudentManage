CREATE TABLE `student` (
                           `id` BIGINT NOT NULL,
                           `name` VARCHAR(255) NOT NULL,
                           `tel` VARCHAR(255) NOT NULL,
                           `email` VARCHAR(255) NOT NULL,
                           PRIMARY KEY (`id`)
);